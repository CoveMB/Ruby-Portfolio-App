
<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @blog.body %>
</p>

<%= link_to 'Edit', edit_blog_path(@blog) %> h if logged_in?(:site_admin) | <%= link_to 'Destroy', @blog, method: :delete, data: { confirm: 'Are you sure?' } %> h if logged_in?(:site_admin) |
<%= link_to 'Back', blogs_path %>
